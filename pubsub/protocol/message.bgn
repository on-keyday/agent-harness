config.go.package = "protocol"
enum MessageKind:
    :u8
    JOIN
    LEAVE

format PubSubRequest:
    config.go.union = "noheap"
    kind :MessageKind
    request_id :u32
    if kind == MessageKind.JOIN:
        nick_name_len :u8
        nick_name :[nick_name_len]u8
    topic :[..]u8

enum Status:
    :u8 
    ok 
    already_subscribed
    unknown_message
    unknown_topic

format PubSubResponse:
    request_id :u32
    status :Status 
    stream_id :u64

